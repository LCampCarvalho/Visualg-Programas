Algoritmo "Tabuada"

Var

n, m, t, i: inteiro


Inicio

escreval("Deseja a tabuada para qual valor? ")
leia(n)

m <- 0
t <- 0

para i de 1 ate 10 faca
      m <- m+1 * n
      t <- t+1
     escreval("4 x ",t, " = " , m )

fimpara




Fimalgoritmo